# Production requirements (lightweight)
# Includes minimal dependencies for ChromaDB + embeddings

# Core API
flask==3.1.2
flask-cors==6.0.1
flask-limiter==4.0.0
gunicorn==23.0.0

# OpenAI
openai==2.7.1

# Vector DB
chromadb==1.3.4

# LangChain
langchain==1.0.3
langchain-community==0.4.1
langchain-core==1.0.3
langchain-huggingface==1.0.1

# Embeddings (required to load vector_db)
sentence-transformers==5.1.2

# Utilities
python-dotenv==1.2.1
pydantic==2.12.4
requests==2.32.5

# Note: torch will be installed as dependency of sentence-transformers
# but Railway cache should make it faster
